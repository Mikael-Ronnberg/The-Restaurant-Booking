export interface IBooking {
  guestFirstName: string;
  phoneNumber: string;
  emailAdress: string;
  partySize: number;
  reservationDate: string;
  reservationTime: string;
}

// guestName,
// guestEmail,
// guestPhoneNum,
// reservationDate,
// reservationTime,
// partySize,
// statusForTable,
